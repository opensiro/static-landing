Archived from commit e84b320 before the Multi-Fidelity Harness CI rewrite.

The HTML references root-level shared assets and styles. `products.css` and
`products.js` are preserved here as the matching page-specific implementation.
